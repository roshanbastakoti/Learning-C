#include <stdio.h>
#include <math.h>
  
int main () {

    int a = 20 ;
    int b = 10 ;
    int k ;

    printf ("the value of a + b is %d\n" , a + b) ;
    printf ("the value of a - b is %d\n" , a - b) ;
    printf ("the value of a * b is %d\n", a * b) ;
    printf ("the value of a / b is %d\n", a / b) ;
    printf ( "5 when divided by 4 leaves a remainder of %d\n", 5%4) ;
    printf ("the value of 4 * 5 is %d\n", 4 * 5) ; 
    printf ("The value of 4 to the power 5 is %f\n", pow (4,5)) ;

    printf ("the value of k is %f\n" , 3.0/9 ) ;










     
return 0;
}