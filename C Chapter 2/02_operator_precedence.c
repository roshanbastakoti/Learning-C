#include <stdio.h>

int main()
{
    int a = 2;
    int b = 4;

    printf("the value of 3 * a + 2 * b is  %d\n", 3 * a + 2 * b);
    printf("the value of 3 * a / 2 * b is  %d\n", 3 * a / 2 * b);
    return 0;
}